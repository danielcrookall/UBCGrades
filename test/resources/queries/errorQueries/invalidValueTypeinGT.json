
{
	"title": "Invalid value type in GT, should be number",
	"input": {
		"WHERE": {
			"GT": {
				"courses_avg": "asdfasd"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_dept",
				"courses_id",
				"courses_year",
				"courses_avg",
				"courses_instructor"
			],
			"ORDER": "courses_year"
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
